apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
   name: chat-route
spec:
   parentRefs:
      - name: gateway
   rules:
      - matches:
           - path:
                type: PathPrefix
                value: "/api/chats"
        backendRefs:
           - name: chat-svc
             namespace: services
             port: 8080
